# TODO: Add generateSKU function to ProductForm.tsx

- [ ] Add the generateSKU function at the top of ProductForm.tsx after imports
- [ ] Modify handleInputChange to auto-generate SKU when title, category, or brand changes (only for existing products with id)
- [ ] Make the SKU input field read-only to prevent manual editing
- [ ] Test the form by editing an existing product to verify SKU auto-generates correctly
