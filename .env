# This is a template file. Copy to .env.local and fill with real values for local development.
# For production, set environment variables in Vercel dashboard.

# Copy this file to .env.local and replace placeholders with your actual values
cp .env .env.local

# Then edit .env.local with your real secrets (it's gitignored)
